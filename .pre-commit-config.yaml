repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
        # Docker Compose merge files use custom tags (!override, !reset) that
        # the default safe loader rejects. --unsafe checks syntax without
        # resolving tags.
        args: [--unsafe]
      - id: check-added-large-files
  - repo: https://github.com/ansible-community/ansible-lint.git
    rev: v25.7.0
    hooks:
      - id: ansible-lint
        exclude: ^\.pre-commit-config\.yaml$
  - repo: https://github.com/pre-commit/mirrors-prettier
    rev: v4.0.0-alpha.8
    hooks:
      - id: prettier
        files: \.ya?ml$
