user  nginx;
worker_processes  auto;

error_log  /var/log/nginx/error.log notice;
pid        /var/run/nginx.pid;


events {
    worker_connections  1024;
}


http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';

    access_log  /var/log/nginx/access.log  main;

    sendfile        on;
    #tcp_nopush     on;

    keepalive_timeout  65;

    #gzip  on;

    include /etc/nginx/conf.d/*.conf;
}

stream {
    log_format proxy '$remote_addr [$time_local] '
                     '$protocol $status $bytes_sent $bytes_received '
                     '$session_time "$upstream_addr"';

    access_log  /var/log/nginx/access.log  proxy;

	include /etc/nginx/stream.d/*.conf;

#     upstream dovecot_imap {
#         server dovecot:143;
#     }
#     server {
#         listen 143 proxy_protocol;
#         proxy_pass dovecot_imap;
#         proxy_protocol on;
#
#         ssl_certificate /etc/nginx/certs/mail.nesono.com.crt;
#         ssl_certificate_key /etc/nginx/certs/mail.nesono.com.key;
#         ssl_dhparam /etc/nginx/certs/mail.nesono.com.dhparam.pem;
#         ssl_trusted_certificate /etc/nginx/certs/mail.nesono.com.chain.pem;
#     }
#
#     upstream dovecot_imaps {
#         server dovecot:1993;
#     }
#
#     server {
#         listen 993 ssl;
#         proxy_pass dovecot_imaps;
#         proxy_protocol on;
#
#         ssl_preread on;
#         ssl_certificate /etc/nginx/certs/mail.nesono.com.crt;
#         ssl_certificate_key /etc/nginx/certs/mail.nesono.com.key;
#         ssl_dhparam /etc/nginx/certs/mail.nesono.com.dhparam.pem;
#         ssl_trusted_certificate /etc/nginx/certs/mail.nesono.com.chain.pem;
#     }
}
